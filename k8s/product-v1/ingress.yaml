apiVersion: networking.k8s.io/v1
kind: Ingress

metadata:
  name: spring-boot-v1-ingress
  namespace: product-v1

spec:
  ingressClassName: nginx

  rules:
    - http:
        paths:
          - path: /v1
            pathType: Prefix
            backend:
              service:
                name: spring-boot-v1-service
                port:
                  number: 9191